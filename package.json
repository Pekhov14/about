{
  "name": "onscrollpathanimations",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "start": "parcel index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/codrops/OnScrollPathAnimations.git"
  },
  "keywords": [],
  "author": "Antony Pekhov",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.6.0"
  },
  "dependencies": {
    "@studio-freight/lenis": "^0.1.2",
    "gsap": "^3.10.4",
    "imagesloaded": "^5.0.0"
  }
}
